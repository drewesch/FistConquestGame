#ifndef ELEMENT_H
#define ELEMENT_H

// Special class that allows a class to reference the enum type if the header file is included.
// Used to simplify code & make the code readable
enum Element {normal, air, water, earth, fire};

#endif